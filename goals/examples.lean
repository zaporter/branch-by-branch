--N: simple not 0 not 2
example : ∀ n, n ≠ 0 ∧ n ≠ 2 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 3
example : ∀ n, n ≠ 0 ∧ n ≠ 3 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 4
example : ∀ n, n ≠ 0 ∧ n ≠ 4 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 5
example : ∀ n, n ≠ 0 ∧ n ≠ 5 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 6
example : ∀ n, n ≠ 0 ∧ n ≠ 6 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 7
example : ∀ n, n ≠ 0 ∧ n ≠ 7 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 8
example : ∀ n, n ≠ 0 ∧ n ≠ 8 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 9
example : ∀ n, n ≠ 0 ∧ n ≠ 9 → Nat.succ (Nat.pred n) = n := by exact?
--N: simple not 0 not 10
example : ∀ n, n ≠ 0 ∧ n ≠ 10 → Nat.succ (Nat.pred n) = n := by exact?
